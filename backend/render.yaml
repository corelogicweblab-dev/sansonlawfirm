# Render reads this when Root Directory = backend
services:
  - type: web
    name: sansonlawfirm-api
    runtime: python
    buildCommand: ./build.sh
    startCommand: uvicorn app.main:app --host 0.0.0.0 --port $PORT
    healthCheckPath: /api/v1/health
