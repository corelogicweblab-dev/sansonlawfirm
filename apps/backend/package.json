{
  "name": "@sanson/backend",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "uvicorn app.main:app --reload --host 0.0.0.0 --port 8100",
    "start": "uvicorn app.main:app --host 0.0.0.0 --port 8100",
    "migrate": "alembic upgrade head"
  }
}
