# SANSON API (FastAPI)

**Render Root Directory:** `apps/backend` (not `backend`)

```bash
pip install -r requirements.txt
uvicorn app.main:app --host 0.0.0.0 --port $PORT
```

Production: https://sansonlawfirm.onrender.com
